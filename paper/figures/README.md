# Figure inputs

The current paper uses two original TikZ diagrams and two product screenshots:

1. `workflow_dag.tex`
   - Vector diagram of the modular workflow architecture.

2. `hitl_workflow.tex`
   - Vector human-in-the-loop legal coding pipeline.

3. `Workflow.png`
   - Product screenshot of the workflow graph.
   - Used to show explicit node types, branches, and the editable workflow representation.

4. `AI_Benchmark.png`
   - Product screenshot of model-level cost, token, benchmark, and variable-level comparison cards.

Do not use the confidential SEAI draft figure in this folder. The TikZ diagram in `hitl_workflow.tex` is an original project-specific replacement.
